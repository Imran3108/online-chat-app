Place your default avatar image `pfp.png` in this folder.

The app uses `/avatars/pfp.png` as the default avatar when a user or friend has no `avatarUrl` set.

Recommended size: 128x128 or 256x256 PNG.